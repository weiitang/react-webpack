import * as React from 'react';

/**
 * tips
 * className
 * FC: children
 */
export type EmptyProps = {
  tips?: React.ReactNode;
  className?: string;
  imgUrl?: string;
  children?: React.ReactNode;
};
